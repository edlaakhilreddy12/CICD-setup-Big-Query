# Configuration for BigQuery CI/CD Pipeline - PRODUCTION Environment
#
# This config is used when deploying to the MAIN/PROD branch
# Changes here deploy to production dataset

# Google Cloud Project Configuration
gcp_project_id: "vast-crow-480921-i7"
dataset_id: "PROD_TEST_CICD_PIPELINE"    # Production dataset
table_id: "employees"
location: "us-central1"

# Data Configuration
data_file: "sample_data.csv"
schema_file: "table_schema.json"

# Environment label
environment: "production"
